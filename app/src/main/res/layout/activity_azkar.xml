
package com.azkari;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;
import androidx.recyclerview.widget.LinearLayoutManager;
import androidx.recyclerview.widget.RecyclerView;

import java.util.ArrayList;

public class AzkarActivity extends AppCompatActivity {

    RecyclerView recyclerView;
    AzkarAdapter adapter;
    ArrayList<String> azkarList;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_azkar);

        recyclerView = findViewById(R.id.recyclerView);
        recyclerView.setLayoutManager(new LinearLayoutManager(this));

        azkarList = new ArrayList<>();
        loadAzkar();

        adapter = new AzkarAdapter(azkarList);
        recyclerView.setAdapter(adapter);
    }

    private void loadAzkar() {
        // مثال: أذكار قصيرة (سأضع لك 60 ذكر لاحقًا)
        azkarList.add("أَصْـبَـحْنا وَأَصْـبَـحَ المُلْـكُ لِلّـهِ ...");
        azkarList.add("اللّهُـمَّ أَنْتَ رَبّـي لا إلهَ إلا أَنتَ ...");
        // ...
    }
}
